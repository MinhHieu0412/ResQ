* Tải permission_handler

* Thêm vào pubspec.yaml 
	
  permission_handler
  device_info_plus
  http
  intl
  image_picker
  webview_flutter
  
  Lấy sử dụng font và icons ở cuối file 
 
//Thêm font ở folder assets/font
//Thêm icon ở folder assets/icons


* Test cấp quyền:
 ##Lần đầu chạy: Hủy 1 quyền > Hiện thông báo chọn mở setting hoặc tắt app > set lại quyền > vào login
 ##Lần 2 test lại: mở setting > uninstall app > chạy lại
 
 ***NÊN GIỮ NÚT MỞ SETTING Ở LOGIN ĐỂ MỞ XÓA APP TEST LẠI (Có thể xóa sau khi bắt đầu chạy lần cuối)
 
 Hiện tại đang hiển thị frontend là do tên project

* Ở main route đang gán id của customer cố định, cần truyền vào

*flutter pub add flutter_dotenv
	
*Chỉnh toDto ở DocumentServiceImpl

*Folder static > Rank data

*Khi tạo mới User thành công > auto tạo 1 dòng trong UserRank || UserRankService.createUserRank(userId)
*Khi kết thúc chuyến > auto gọi update điểm user (có kiểm tra rank) || CustomerService.updateCustomerPoint(rrId)

*Remove hết hạn: khi dùng discount xong trừ SL > check quantity = 0 gọi UserDiscount.removeExpiredDiscounts(discountId)


AndroidManifest

<!-- INTERNET -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- CAMERA -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- LOCATION -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- IMAGE -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
	
	trong queries:
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>